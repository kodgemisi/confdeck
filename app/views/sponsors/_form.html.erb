<%= form_for([@conference, @sponsor], :html => {:class => 'form-horizontal'}) do |f| %>
  <% if @sponsor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sponsor.errors.count, "error") %> prohibited this sponsor from being saved:</h2>

      <ul>
      <% @sponsor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="col-md-4">

    <div class="form-group">
      <%= f.label :name, :class => "control-label col-lg-3" %>
      <div class="col-lg-9">
        <%= f.text_field :name, :class => "form-control" %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :website, :class => "control-label col-lg-3" %>
      <div class="col-lg-9">
        <%= f.text_field :website, :class => "form-control" %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :logo, :class => "control-label col-lg-3" %>
      <div class="col-lg-9">
        <%= f.file_field :logo, :class => "form-control" %>
      </div>
    </div>


    <div class="actions">
      <%= f.submit :class => "btn btn-default pull-right" %>
    </div>
    
  </div>
</div>

<% end %>
